# Discord Bot — Feature Log

> All items below were **built and `node --check`-green** on the dates shown. The bot has not yet been pushed live, so each "Date & Time Deployed" is the **build date** — replace it with the actual deploy time when you restart/redeploy (e.g. `June 24, 2026 11:31 pm`). When the whole bot ships at once, you may instead stamp them all with that single deploy time.

---

Feature/Change: Bot scaffold & infrastructure — discord.js v14 (CommonJS), dynamic command/event loaders, `/ping`, slash commands auto-register on every startup, Railway deploy prep (DEPLOY-RAILWAY.md, .railwayignore, git initialized, .env untracked)
Date & Time Deployed: June 22, 2026

Feature/Change: `/guildapplication` — public "Start Application" button → modal (In-game Name / Playstyle / Previous Guild / Inviter) → posts the application embed; auto-detects the applicant's class from their onboarding role; reviewer-gated 4-button review (Main Guild → Daddy, Second Guild → Mummy, Waiting List, Reject) with ID-based role assignment and generic-polite DMs
Date & Time Deployed: June 22–23, 2026

Feature/Change: Job-Ad & Officer Application (`/jobad`) — leadership-gated; modal (Title + Description) → posts a branded job-ad embed to #job-ad with an Apply button → applicant modal (In-game Name + why-fit) → posts to #officer-applications with Daddy / Mummy / Reject review (leadership-gated); approval assigns the officer role, deletes the job-ad, and DMs the applicant. Replaced the earlier `/officerapplication` + `/openapplication` commands (retired)
Date & Time Deployed: June 23, 2026

Feature/Change: `/memberclasses` — officer-gated, public result; "All" view shows per-class Daddy vs Mummy member counts; per-class view lists member nicknames by group (Daddy / Mummy), length-guarded
Date & Time Deployed: June 23, 2026

Feature/Change: `/help` — open to everyone, ephemeral, role-filtered categories (General / Leadership / Officers); branded embed that lists only the commands the member can actually use
Date & Time Deployed: June 23, 2026

Feature/Change: Welcome-on-join — new members trigger a welcome embed in #welcome (requires the Server Members Intent enabled in the Developer Portal)
Date & Time Deployed: June 23, 2026

Feature/Change: Party Finder (`/partyfinder`) — gated to PARTYFINDER_ROLE_IDS (Daddy/Mummy member + Officer Daddy/Mummy); replies EPHEMERALLY to the invoker with a prompt + Start Party / I Need Carry buttons (no persistent channel post — see the UX-change entry below). **Start Party:** class-role gate → party-size select (5/10/Custom→modal) → Party Details modal (event/time/power, don't-overstate reminder) → Roles Needed modal (Tank/Heal/DPS, must sum to size & leader's own category ≥1) → public party card with class-matched Join as Tank/Heal/DPS + leader-only Cancel; auto-locks "PARTY FULL" (green) when filled. **I Need Carry:** details modal → public carry card with "I'll carry this" (Carry-role-gated, deduped) + requester-only Cancel. Both posts auto-expire & delete after 10 min if unfilled. Ported from the reference discord.py bot; new `partyfinder/` module (constants/handlers/state) + interactionCreate router wiring (now also routes select menus). In-memory state (Maps), lost on restart — accepted v1 tradeoff. Requires **Manage Messages** in #party-finder (for expiry deletes) + Send Messages/Embed Links + Server Members Intent (already on). NO Manage Roles needed (reads roles only).
Date & Time Deployed: June 24, 2026 (built & static-verified — `node --check` green on all files; not yet live-tested)

Feature/Change: Party Finder fix — shortened 4 modal TextInput labels that exceeded Discord's 45-char limit (power rating + the 3 Tank/Heal/DPS role-count labels), which were crashing `showModal` on party-size pick; intent moved into placeholders (≤100). Audited all partyfinder labels/titles/placeholders/buttons/select-options/embed-field-names against Discord limits — all within bounds; all 6 ModalBuilders verified via `.toJSON()`. Labels/placeholders only, no flow change.
Date & Time Deployed: June 24, 2026 (fix; static + builder-construction verified)

Feature/Change: Party Finder UX change — dropped the persistent entry card. `/partyfinder` now replies EPHEMERALLY to the invoker with the prompt + Start Party / I Need Carry buttons (only they see it) instead of posting the "🎮 Party Finder" embed to #party-finder. Clicking either button proceeds into the existing flows unchanged. Command-level PARTYFINDER_ROLE_IDS gate retained. Party/carry cards still post publicly to #party-finder from within the flows — unchanged. Router unchanged (identical button customIds work from an ephemeral message).
Date & Time Deployed: June 24, 2026 (UX change; static-verified)

Feature/Change: Party Finder fix — eliminated illegal modal→modal chaining that threw `interaction.showModal is not a function` live (Discord forbids showing a modal in response to a modal-submit). Start Party flow restructured: party-size **select** → Details modal (preset bakes size in customId; **Custom** folds a "Party size (1-50)" field into the same modal — the separate custom-size modal is gone) → Details modal-submit now **replies** with a "Set role counts" bridge button → that **button** opens the Roles Needed modal → posts the party card. Zero modal→modal anywhere. Bot-wide showModal audit: all 6 calls (partyfinder ×3, officerapp, guildapp, /jobad) confirmed reachable only from Button/Select/Command. No file deletions.
Date & Time Deployed: June 24, 2026 (fix; static + builder-construction + router-trace verified)

Feature/Change: Party Finder Start-Party enhancements (5; party flow only) — (1) recruitment window 10→15 min. (2) On expiry the party post is KEPT (not deleted): embed flips grey to "⌛ Recruitment Closed", countdown → "Recruitment closed", buttons removed; carry still deletes on expiry. (3) Live recruitment countdown as an embed FIELD `<t:expiry:R>` (self-updating client-side) + absolute GMT+7 close time in the footer. (4) When a party fills, the bot posts a NEW message tagging every joined member (real pings) with the event + GMT+7 start time + "be on time". (5) Server start time is now a GMT+7 15-min-increment DROPDOWN (replaces the free-text field): new flow Start→size select→**GMT+7 time select**→Details modal (event + power; custom also size; no time field)→"Set role counts" bridge button→Roles modal→post. Slots: earliest = round-up(now+30min) to the next 15-min boundary, exactly 25 ascending options, GMT+7 "h:mm AM/PM" labels (next-day prefixed). Party stores the GMT+7 label + start epoch + expiry epoch; embed shows "🕒 Start Time" (GMT+7) + `<t:start:R>`. Still zero modal→modal (time-select sits before the modal); showModal audit re-confirmed clean. Carry flow unchanged (still free-text time, still deletes on expiry). No file deletions.
Date & Time Deployed: June 24, 2026 (enhancements; static + slot-gen + router-trace verified)

Feature/Change: Party Finder embed tweak — the "⏳ Recruitment closes" live countdown field now sits at the very bottom of the party embed (after the Tank/Heal/DPS slots) instead of higher up. Value and per-state behavior unchanged (open shows it last; full/cancel omit it; expire shows a static "Recruitment closed" last).
Date & Time Deployed: June 24, 2026 (tweak; field-order verified)

Feature/Change: Party Finder carry-flow parity + "Server time" wording. Carry now matches Start Party where it maps: (1) free-text time replaced by the same GMT+7 15-min time dropdown — I Need Carry button → ephemeral time select → Carry Details modal (event name only) → post (button→select→modal, zero modal→modal). (2) On expiry the carry post is KEPT (not deleted): grey "⌛ Carry Request Closed" embed, countdown dropped, buttons removed — same as party. (3) Live "⏳ Request closes" `<t:R>` countdown field at the bottom + absolute close time in footer. (4) "🕒 Start Time" field shows the GMT+7 label + `<t:start:R>`. Carry-role gate, responder list, and requester-Cancel unchanged; no full-ping/roles/size (don't apply to carry). Separately, all user-facing time displays in BOTH flows now read "Server time (GMT+7)" (start-time fields, the full-party ping, time-select placeholders, footer close notes). No file deletions.
Date & Time Deployed: June 24, 2026 (carry parity + wording; field-order + router-trace verified)

Feature/Change: Party Finder — free role choice + role-switching on Join. The class-lock on JOINING is dropped: a member can join any needed, non-full slot (Tank/Heal/DPS) regardless of class, and can switch between slots freely (vacates the old slot, takes the new). Joining a slot you're already in is a no-op; switching into a full slot is refused and you keep your current slot. Party fills/locks/pings as before. The leader Start-Party flow (class gate + class-based auto-seat) is unchanged for now. New state helpers currentCategory + removeMember. No file deletions.
Date & Time Deployed: June 24, 2026 (join/switch; state-sim verified)

Feature/Change: Party Finder — recruitment now closes 15 minutes BEFORE the chosen start time (both party and carry), instead of a fixed 15-min window after posting. expiryEpochSecs = startEpochSecs − 15 min; the auto-close timer fires at that instant (delay clamped ≥0). The live countdown and footer close-time automatically show the new "closes 15 min before start" value; the "🕒 Start Time" field still shows the start itself. Constant PARTY_TIMEOUT_MINUTES renamed to EXPIRY_LEAD_MINUTES_BEFORE_START (still 15). On-close behavior unchanged (grey "Closed" embed kept, countdown dropped, buttons removed). No file deletions.
Date & Time Deployed: June 24, 2026 (expiry rule; computation + timer-delay verified)

Feature/Change: Kudos Board — a recognition system with chat-message giving + **MongoDB Atlas** persistence. **Give:** chat `kudos @member [reason]` (NOT a slash command) — up to 7 per giver per day (resets midnight GMT+7), multi-mention supported, no self/bot kudos, optional reason stored. The bot replies confirming + pings the recipients. **`/kudosboard`** (public) — top 15 recipients by total, plus your own rank if you're below 15. **`/profile [user]`** (public) — avatar, total kudos received, rank, and kudos given today (Y/7). **Persistence:** MongoDB Atlas (`discordbot.kudos` collection, one doc per kudo `{ guildId, giverId, recipientId, reason, createdAt }`) via `MONGODB_URI`; indexes auto-create on boot (idempotent). **Graceful degradation:** if no DB is configured/reachable, the bot still runs fully and the kudos surfaces reply "not configured yet." New files: kudos/db.js, events/messageCreate.js, commands/kudosboard.js, commands/profile.js, KUDOS-DEPLOY.md. Touched: index.js (+GuildMessages/MessageContent intents), events/ready.js (store init), commands/help.js (new Community group), .env.example, DEPLOY-RAILWAY.md. Added `mongodb` dependency. ⚠️ Requires Conrad to (a) set `MONGODB_URI` (same Atlas SRV string locally + in Railway) and allow Atlas Network Access from `0.0.0.0/0`, and (b) enable Message Content Intent in the Dev Portal — see KUDOS-DEPLOY.md. Live smoke test confirmed Atlas connectivity + index creation. No file deletions.
Date & Time Deployed: June 24, 2026 (built; Atlas connectivity + indexes smoke-tested GREEN; Railway MONGODB_URI + Message Content Intent + 0.0.0.0/0 pending Conrad — not yet end-to-end live-tested in Discord)

Feature/Change: Party Finder — Join is CLASS-BASED again, now multi-role (replaces the brief "any role freely switchable" experiment; switching is kept but constrained to a class's allowed roles). Each class maps to an ordered list of categories it may apply for: Knight → Tank/DPS, Priest → DPS/Heal, all other classes (Assassin/Blacksmith/Druid/Gunslinger/Hunter/Wizard) → DPS only. A member must hold a class role to join, may join/switch only to categories their class can fill (e.g. Knight switches Tank↔DPS, a DPS class clicking Tank is refused with "you can apply as: DPS"), and the existing switch/full/already-in-slot behavior is unchanged otherwise. Leader: must hold a class role to start; their party-card slot is auto-filled into the first of their class's roles (in listed order) that has an open slot; the Roles form must include at least one slot a leader's class can fill. Druid is confirmed DPS. No file deletions.
Date & Time Deployed: June 24, 2026 (class-based multi-role join + leader seat; eligibility matrix + leader-seat sim verified)

Feature/Change: Class Quiz Bot (`/qna`) — a multiple-choice quiz running one independent loop per class channel (knight / priest / wizard ONLY — the 3 populated banks; assassin/blacksmith/gunslinger/hunter banks are empty, druid has none), **active hours 11:00–21:00 GMT+7 daily** (⚠️ TEMP TESTING — end extended from 7 PM to 9 PM for testing; revert `QUIZ_ACTIVE_END_HOUR` to 19 before go-live) (finish-open-then-pause: a question already posted runs its full 30-min window even if reveal crosses 19:00; the start-gate fires only at the top of each loop iteration after the previous reveal; outside the window the loop sleeps to the next 11:00 GMT+7; recover-on-boot closes persisted open questions regardless of the clock). Each loop posts a question (📝 gold embed + 4 answer buttons `quiz:answer:<LETTER>`), holds it open 30 min, then edits the message to reveal the correct answer + everyone who got it right (⏰ green embed), then checks the active-hours gate before the next question. **Answering:** correct → ephemeral "✅ Correct!" AND your server nickname is added to the live public "✅ Correct so far" list (message edited); wrong → PRIVATE ephemeral "❌ Wrong! The correct answer is **X. …**" with NO public change (others can't tell you missed). One answer per user per question. Points tracked per member (`quiz_scores`); **leaderboard and `/qna` display the stored `username` field, which is now the server nickname** (`interaction.member?.displayName`) consistent with the correct-so-far list — a single `addPoint` call self-updates the stored name on every answer so scores track the current nickname over time; single self-updating live `#leaderboard` message (keyed by guildId); **`/qna`** posts a PUBLIC top-10 (added to `/help` under a new everyone-access "🎯 Quiz" category). **Recover-on-boot** (Conrad's decision, NOT the source's "soft reset"): on startup the bot re-scans persisted open questions (`quiz_active`) and resumes each — if its 30-min window already elapsed it reveals immediately, else it reschedules the reveal for the remaining time — then resumes normal looping; the reveal is reconstructed purely from persisted state (question text / 4 options / correct letter / messageId), so it survives a redeploy even if the question bank changed. **Persistence:** MongoDB Atlas on the SAME cluster as Kudos (db `discordbot`, new collections `quiz_scores` / `quiz_active` / `quiz_leaderboard_msg`; reuses `MONGODB_URI`; idempotent index creation on boot). **Graceful degradation:** no `MONGODB_URI` or Atlas unreachable → quiz disabled, bot STILL boots fully (Party Finder / Kudos unaffected); answer buttons and `/qna` reply "not available/configured." Ported from the reference Python/discord.py/SQLite bot (`inbox/discord-bot/function-2/`) with deviations: channels matched by hard-coded ID (not name), MongoDB (not SQLite), and the recover-on-boot resume. ⚠️ The reference CSV's header is `correct answer` (space) and its "correct answer" column stores the option TEXT, not a letter — the source Python loader (which requires `correct_answer` and a letter) would have crashed on its own data; the question-bank generator normalizes the header and maps each correct-answer text to its A/B/C/D slot (verified counts knight=41 / priest=32 / wizard=34, 0 mismatches). New `quiz/` module (constants/db/handlers/questions.js) + `commands/qna.js`; interactionCreate router wiring (quiz.route, unique `quiz:*` namespace); ready.js boot (init + recover + start loops, after the kudos block). No new gateway intent (buttons only — Message Content NOT needed). No package.json change (`mongodb` already a dependency). Requires (Conrad's side): bot has **Send Messages + Embed Links + Read Message History** in the 3 class channels + #leaderboard (NO Manage Roles, NO Manage Messages); `MONGODB_URI` already set from Kudos (reused, nothing new); restart. No file deletions.
Date & Time Deployed: June 25, 2026 (built & static-verified — `node --check` green on all 8 files; scratch audit 27/27 green: CSV→data counts + correct-letter mapping, the 3 recover-on-boot branches, answered-guard/one-per-user, wrong-answer privacy, graceful degrade; not yet live-tested; 3 quiz/handlers.js tweaks 2026-06-25: (A) leaderboard/scores use server nickname — `addPoint` now stores `interaction.member?.displayName` consistent with the correct-so-far list; (B) open-question embed drops the redundant A/B/C/D option-choice fields — buttons already show the full option text, max label 71 chars across all 107 questions, well under the 80-char button cap; (C) window-end and recover-on-boot now DELETE the question message instead of editing to a reveal embed — Conrad's request to keep channels clean; deleting bot's own message needs no extra permission; `buildRevealEmbed` left on disk per §1.1; running scores still live on #leaderboard / /qna)

Feature/Change: Class Quiz Bot — answer window extended 30 min → 1 hour; active-hours testing end extended 9 PM → midnight (12 AM) GMT+7. `QUESTION_WINDOW_MS` in `quiz/constants.js` changed from `30 * 60 * 1000` to `60 * 60 * 1000` (3600000 ms). Footer copy in `quiz/handlers.js` updated to "You have 1 hour to answer." All 30-min/30-minute references in comments swept and updated. `QUIZ_ACTIVE_END_HOUR` changed from 21 → 24 (gate is end-exclusive: hours 11–23 active, 00:00/midnight = paused — no wrap-around needed). Both changes are TEMP TESTING values; still revert to `QUIZ_ACTIVE_END_HOUR = 19` before go-live. `node --check` GREEN on both files; no remaining "30 min" / "30-minute" strings in quiz/*.js.
Date & Time Deployed: June 25, 2026 (staged; `node --check` green; not committed/pushed; not live-tested)

Feature/Change: Maintenance script — `scripts/clear-quiz-leaderboard.js`. Wipes the quiz leaderboard (`quiz_scores` collection) with a safety dry-run gate. Dry run (default): counts and prints what would be deleted, exits without touching the DB. Live wipe: requires `--yes` flag. Optional `--reset-message` flag additionally clears `quiz_leaderboard_msg` so the bot posts a fresh #leaderboard message on the next question close (off by default). Does NOT touch `quiz_active`. Does NOT edit/delete the live Discord message. Env loaded via `require('dotenv').config()` (matches index.js). Commands: dry run = `node scripts/clear-quiz-leaderboard.js`; live = `node scripts/clear-quiz-leaderboard.js --yes`; Railway = `railway run node scripts/clear-quiz-leaderboard.js --yes`. `node --check` GREEN.
Date & Time Deployed: June 25, 2026 (built; `node --check` green; not committed/pushed; NOT to be run until Conrad is ready to wipe scores)

Feature/Change: Class Quiz Bot — expanded to full 7-class bank + new channel IDs. `quiz/questions.js` regenerated from `questions-2.csv` (supersedes the old 3-class bank): knight=42, priest=33, wizard=35, assassin=29, blacksmith=28, gunslinger=28, hunter=23 (218 usable questions; 2 broken hunter rows skipped — see below). `quiz/constants.js` QUIZ_CHANNELS expanded to 8 entries: the 3 original + assassin (1518082757425762435), blacksmith (1518082722852114564), hunter (1518082636663488532), gunslinger (1518082820151574630), and druid (1518280099043217508) — druid has NO question bank yet; getRandomQuestion('druid') returns null so the loop warns and exits gracefully, leaving #druid idle until a druid bank is added. Thief→assassin alias baked into the generator (zero thief rows in current source). Max option button label: 71 chars across all 218 questions — well within the 80-char Discord cap. `node --check` GREEN on both files. **Data quality note:** 2 hunter rows in questions-2.csv are broken and were skipped: (1) [WIND WALKER lv10] — correct_text='30' but options are 35/40/45/50 (correct answer not present in options); (2) [FALCON ASSAULT - ULTI SKILL] — all four option fields are empty (likely spreadsheet export artifact). Hunter bank is 23 (not the expected 25) until the CSV is corrected. 4 new channels (assassin/blacksmith/hunter/gunslinger) need Send Messages + Embed Links + Read Message History bot perms + a restart to go live.
Date & Time Deployed: June 26, 2026 (staged; `node --check` green on questions.js + constants.js; not committed/pushed; not live-tested)

Feature/Change: Kudos Board — `/kudosboard` now shows TWO sections separated by a `━━━━━━━━━━` divider: **🗓️ Today** (top-10 recipients since GMT+7 midnight) and **🏆 All-Time** (top-10 overall). Daily-empty shows `_No kudos yet today — be the first!_` while all-time still lists; all-time empty shows existing empty-state text. Whole-board empty (both empty) keeps the single "No kudos yet" embed. All-time "your rank" line preserved. New `topRecipientsToday()` function + `{ guildId, createdAt }` compound index added to `kudos/db.js`. `node --check` GREEN; sanity audit 5/5 PASS.
Date & Time Deployed: June 27, 2026 (staged; `node --check` green; not committed/pushed; not live-tested)

Feature/Change: Member-data sync — new `membersync/` module. Stores the live Daddy (main) + Mummy (sub) roster to MongoDB (`discordbot.members` collection) on an **hourly timer** (setInterval, 1 h) and via a new **officer-only `/syncmembers`** slash command. Own MongoClient (separate from kudos/quiz). Graceful degrade: no `MONGODB_URI` or Atlas unreachable → sync disabled, bot still boots, `/syncmembers` replies "Member sync isn't configured yet." **Member doc schema:** `{ userId, username, displayName, avatarUrl|null, isMain (bool: Daddy/ACCEPTED role), isSub (bool: Mummy role), className|null, classRoleId|null, updatedAt: Date }`. Role IDs imported from `guildapp/constants.js` (ROLE_IDS.ACCEPTED + ROLE_IDS.MUMMY). Class resolved via CLASS_ROLE_BY_ID (8 classes). Officer gate: OFFICER_ROLE_IDS from `officerapp/constants.js` (identical to /memberclasses pattern). `removeStale` deletes docs whose userId is no longer in the qualifying set (empty-roster guard prevents full wipe). New files: `membersync/{db,sync,index}.js`, `commands/syncmembers.js`, `scripts/sanity-membersync.js`. One additive edit to `events/ready.js` (require + initAndStart call after the quiz block). GuildMembers intent already enabled — no Discord-side changes needed. `node --check` GREEN all files. Smoke: 10 commands + 4 events load with and without MONGODB_URI. Sanity: 52/52 PASS.
Date & Time Deployed: June 27, 2026 (staged; `node --check` green; smoke + sanity verified; not committed/pushed; not live-tested)

Feature/Change: Class Quiz Bot — active-hours end extended to 3:00 AM GMT+7 with midnight-crossing gate logic. `quiz/constants.js`: `QUIZ_ACTIVE_END_HOUR` changed 24 → 3. Testing window is now **11:00 AM–3:00 AM GMT+7 (crosses midnight)** — ⚠️ TEMP TESTING; revert to 19 (7 PM) before go-live. `quiz/handlers.js`: `isActiveHour()` rewritten to support midnight-crossing windows — when `END_HOUR <= START_HOUR`, condition is `h >= START || h < END` (midnight-crossing); otherwise `h >= START && h < END` (same-day, e.g. production END=19). Result: hours 11–23 AND 0, 1, 2 ACTIVE; hours 3–10 PAUSED. `nextActiveStartMs()` unchanged — confirmed correct for all paused hours in [3, 11). `node --check` GREEN on both files. Scratch audit 21/21 PASS: all active/paused boundary hours, next-start epochs (05:00→today 11:00/6h; 10:59→today 11:00/1min), finish-open-then-pause (Q posted 02:59, reveal 03:59, gate fires h=3 paused, sleeps to next 11:00), END=19 regression (same-day branch correct across all boundary hours). Still STAGED — not committed/pushed, not live-tested.
Date & Time Deployed: June 26, 2026 (staged; `node --check` green; not committed/pushed; not live-tested)
